/// Create Event
visible = false;     
speed = 0.8;                                // Movement speed
direction = choose(0, 90, 180, 270);        // Start moving in a random direction
alarm[0] = 120;                              // Change direction randomly every 2 seconds

