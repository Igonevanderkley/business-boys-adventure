// Left Pressed Event
/*
if (sound_on) {
    // Zet geluid uit
    sound_on = false;
    sprite_index = spr_sound_off;
    audio_master_gain(0, 0, 0); // alle geluid uit
} else {
    // Zet geluid aan
    sound_on = true;
    sprite_index = spr_sound_on;
    audio_master_gain(0, 1, 0); // alle geluid aan
}
*/
